What happens when you initialize a repository? Why do you need to do it?

A .git folder with metadata is initialized. This is necessary for storing project files with git.

How is the staging area different from the working directory and the repository? What value do you think it offers?

The staging area is a temporary zone for you to have more control over which files you commit.